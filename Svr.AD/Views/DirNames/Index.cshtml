@model Svr.AD.Models.DirNameViewModels.IndexViewModel

@{
    ViewData["Title"] = "Список справочников";
}

<h4>@ViewData["Title"]</h4>
@await Html.PartialAsync("_StatusMessage", Model.StatusMessage)
<p>
    @*<a asp-action="Create" asp-controller="Regions" class="btn btn-default">Добавить</a>*@
    @await Html.PartialAsync(partialViewName: $"_AddButton")
</p>
@await Html.PartialAsync("List", Model)